# Chave da OpenAI (sem prefixo Bearer)
OPENAI_API_KEY=sk-...

# Host/porta da API
PORT=3000          # porta interna do app
HOST=0.0.0.0
API_PORT=8000      # porta exposta no host/VPS (agora 8000)

# Chroma remoto (deixe vazio para usar o Chroma local persistente em ./chroma)
# CHROMA_HOST=http://<ip-da-vps>:8000

# Ajustes do RAG
# ADK_CHUNK_SIZE=800
# ADK_CHUNK_OVERLAP=200
# ADK_TOP_K=5
# ADK_COLLECTION_NAME=local_docs
# ADK_CHROMA_PATH=chroma

# LiteLLM / OpenAI
# OPENAI_BASE_URL=
# OPENAI_API_VERSION=
